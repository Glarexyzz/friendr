cache:
  paths:
    - node_modules/

local_deploy:
  stage: deploy
  script: 
    - npm install
    - npm run dev -- --host

remote_deploy:
  stage: deploy
  script:
    - npm install
    - tsuru app deploy --app drp57 ./

test:
  stage: test
  script: echo "Testing"
  environment: test
